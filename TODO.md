# TODO (potential new stuff)
- [x] Add the ability to generate expressions for characters using comfyui. (using one of those workflows)
- [x] Improve the UX since it is quite ugly and confusing. Maybe we should move the UI into a proper manu on its own, using tabs etc.
- [x] Add a button to generate the visual identity of the character from the character's description
- [x] Make sure you aren't just storing stuff in browser storage but like are storing things server-side so that if I login to another machine, the config is still there.
- [x] The visual identity thing doesn't seem to do anything.
- [x] I want a proper lora manager with picking files, enabling/disabling, etc
